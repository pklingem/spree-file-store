%h1= t('upload_a_new_file')

- form_for(:uploaded_file, :url => { :action => "create" }, :html => { :multipart => true }) do |form|
  %fieldset
    = form.file_field :uploaded_data
    .upload_button
      = button t('upload_file') 
